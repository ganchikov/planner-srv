export enum Collections {
    teams = 'team',
    people = 'person',
    absences = 'absence',
    counters = 'counter',
}

export enum TeamDocumentFields {
    members = 'members',
}

export enum PersonDocumentFields {
    absences = 'absences',
}